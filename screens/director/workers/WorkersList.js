import React, { useEffect, useState, useCallback } from "react";
import {
   View,
   Text,
   FlatList,
   Button,
   StyleSheet,
   ActivityIndicator,
   TouchableOpacity,
   Alert,
} from "react-native";

const WorkersList = () => {
   return (
      <View>
         <Text>WorkersList</Text>
      </View>
   );
};

export default WorkersList;
