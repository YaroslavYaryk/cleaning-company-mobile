import React, { useEffect, useState, useCallback } from "react";
import {
   View,
   Text,
   FlatList,
   Button,
   StyleSheet,
   ActivityIndicator,
   TouchableOpacity,
   Alert,
} from "react-native";

const WorkList = () => {
   return (
      <View>
         <Text>WorkList</Text>
      </View>
   );
};

export default WorkList;
